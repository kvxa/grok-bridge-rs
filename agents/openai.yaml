interface:
  display_name: "Grok Build"
  short_description: "Delegate and monitor Grok Build coding tasks"
  default_prompt: "Use $grok-build to start this implementation in Grok, monitor it with read/wait, then inspect and verify the changes."

policy:
  allow_implicit_invocation: true
